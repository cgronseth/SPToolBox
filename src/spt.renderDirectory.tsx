import * as React from "react";
import * as ReactDOM from "react-dom";

import { Directory } from "./components/spt.directory";

ReactDOM.render(
    <Directory />, document.getElementById("spt.directory")
);